Ubuntu Server 16.04 LTS (HVM), SSD Volume Type - ami-5e8bb23b

sudo groupadd -g 420 kodi
sudo useradd -c 'kodi user' -u 420 -g kodi -G audio,video \
   -d /var/lib/kodi -s /usr/bin/nologin kodi
sudo passwd -l kodi > /dev/null
sudo mkdir /var/lib/kodi/.kodi
sudo chown -R kodi:kodi /var/lib/kodi/.kodi

sudo apt-get update
sudo apt-get install software-properties-common
sudo add-apt-repository ppa:team-xbmc/ppa
sudo apt-get install kodi xorg xserver-xorg-legacy dbus-x11 alsa-utils
sudo apt-get update && upgrade

nano 
'
[Unit]
Description=Kodi standalone
After=systemd-user-sessions.service network-online.target sound.target mysqld.service
Requires=network-online.target
Conflicts=getty@tty1.service

[Service]
User=kodi
Group=kodi
PAMName=login
TTYPath=/dev/tty1
ExecStart=/usr/bin/xinit /usr/bin/kodi-standalone -- :0 -nolisten tcp vt1
Restart=on-abort
StandardInput=tty

[Install]
WantedBy=graphical.target
'
sudo systemctl enable kodi
sudo service kodi status
sudo apt-get install s3fs


sudo service kodi start


.passwd-s3fs









